#!/bin/bash
set -e

SERVICE_NAME=stun
BASE_DIR=<%= node.stun.base_dir %>
PID_FILE=<%= node.stun.pid_file %>
LOGS=<%= node.stun.logs %>
CMD=${BASE_DIR}/bin/generic_start.sh  

rm -f <%= node.stun.pid_file %>
$CMD $SERVICE_NAME $BASE_DIR $PID_FILE $LOGS